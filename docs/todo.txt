- Create a 'version' record
    1. Create a txt file to record downloaded tags. Give it the name 'godot_remote_tags_dowloaded.txt'
        -> Check if already exist. If not, create a new.
    2. Save the downloaded tag on the txt file.
    3. Change the condition to download to find if the Godot version is already downloaded.
- Create a 'delete' function.
    This function will delete the downloaded Godot version previously donwloaded.
    1. Search the Godot version on the 'godot_remote_tags_dowloaded.txt'.
        - If found, delete the godot version.
        - If not, print a msg.
- Make a 'force' option on download and delete.
    1. If force is given for download, do not check if the godot version is already downloaded.
        -> Print a user msg explaining.
    2. If force is given for delete
        - Check for a regex with the tag. Probably will found as the name follows a pattern.
            -- If not found, print a msg.
        - Delete all version.
- Save Godot on a separate folder